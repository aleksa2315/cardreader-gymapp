server:
  port: ${PORT:8080}
  address: 0.0.0.0

spring:
  datasource:
    url: jdbc:mysql://mysql-cardreader.alwaysdata.net:3306/cardreader_membership?useUnicode=true&characterEncoding=utf8&serverTimezone=Europe/Belgrade&sslMode=REQUIRED&allowPublicKeyRetrieval=true
    username: cardreader
    password: Aleksa21
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: ${JWT_SECRET:CHANGE_ME_CHANGE_ME_CHANGE_ME_CHANGE_ME_1234567890}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:720}

admin:
  username: ${ADMIN_USERNAME:admin}
  password: ${ADMIN_PASSWORD:admin123}